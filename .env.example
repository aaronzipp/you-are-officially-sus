# Copy this file to `.env` and fill in values before running the stack.
# The DB password is required by docker-compose to initialize Postgres.
DB_PASSWORD=change-me

# Leave empty to disable verbose logging; set to any non-empty value to enable.
DEBUG=
